<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.handyhelp.handlyhelp">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name="android.support.multidex.MultiDexApplication">
        <activity android:name=".SplashActivity" android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity"/>

        <activity android:name=".RegistrationLoginOptions"/>

        <activity android:name=".LoginSignUp"/>

        <activity android:name=".users.SignUpActivity"/>

        <activity android:name=".users.LoginUsers"/>

        <activity android:name=".users.HelperListing"/>

        <activity android:name=".users.ServiceListing"/>

        <activity android:name=".helpers.LoginHelper"/>

        <activity android:name=".helpers.RegisterHelper"/>

        <activity android:name=".helpers.LoginRegistrationOptions"/>

        <activity android:name=".users.BookedListing"/>

        <activity android:name=".helpers.HelperMainPage"/>

        <service android:name=".helpers.LocationTrackerService"/>

        <activity android:name=".users.BookingDetails"/>

        <activity android:name=".users.PaymentSuccess"/>
    </application>

</manifest>